#include<iostream>
using namespace std;
int main()
{
	int age;
	cout<<"Enter the age of the candidate: ";
	cin>>age;
	if(age>=18)
	cout<<"Candidate is elegible to vote!";
	else
	cout<<"candidate is not elegible to vote!";
	return 0;
	
}
